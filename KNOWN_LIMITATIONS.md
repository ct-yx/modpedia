# v0.2.0 已知限制

- 手册扫描只读取整合包中已安装 JAR 的本地资源，不联网下载手册。
- 三个手册框架本身不包含内容时只会作为依赖型 JAR 统计；可检索正文必须来自内容模组。
- 不同版本或第三方改版可能改变手册资源路径，导致扫描覆盖率不同。
- 来源跳转依赖目标手册模组的客户端公开入口；入口不存在时仍可查看来源预览，但无法直接跳转。
- AI 回答需要玩家自行提供兼容 API；网络质量、模型能力和服务端限额不属于 ModPedia 控制范围。
- FTB Quests、JEI、Jade 不是必需依赖；它们缺失时对应任务、配方跳转或视线目标按钮不会出现。不同版本的公开 API 可能使对应联动暂时不可用。
- FTB Quests 当前通过客户端反射读取运行时快照，无法读取到快照时不会猜测玩家进度；任务 Wiki 网络更新失败时继续使用内置或本地缓存。
- Jade 当前优先使用目标方块的物品表示作为兜底，复杂实体、流体或自定义提示不一定能转换为物品 ID。
- JEI 配方不导入 SQLite，只在运行时按物品 ID尝试打开输出配方界面；JEI 未就绪或 API 变化时保留正文文本。
- 当前仅搜索模式使用规则检索，不进行语义向量扩展；可用模组 ID、物品 ID、机器名、中文/英文关键词提高命中率。
- `knowledgeBenchmark` 的冷查询是每次新建 JDBC Reader 的连接冷启动，不会清空 macOS/操作系统页缓存；10× 报告用于相对比较，不等同于所有大型整合包的硬件实测。
- 首次启动或 F9 重建会在后台生成 SQLite 派生库，重建完成前搜索可能显示知识库尚未生成。
- 当前早期测试版发现旧 Schema 或损坏 `knowledge.db` 时直接删除派生数据库并全量重建，不提供旧数据库迁移；原始 JAR、Markdown 和 Wiki 源文件会保留。
- Beta 尚未覆盖所有大型整合包的完整手册资源格式，建议保留 `config/modpedia/knowledge/custom/` 中的人工补充文档。
